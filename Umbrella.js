class Umbrella{
    options = {
        constructor(){
            this.position.x; pointA,
            this.position.y; pointB
        }
    }

    umbrella = addImage("images/Walking-Frame/walking_1.png", umbrella_img);
    umbrella = loadImage(umbrella_img);
}